# Core dependencies
numpy>=1.24.0,<2.0.0
PyQt6>=6.5.0

# Visualization dependencies (required for 3D viewer)
matplotlib>=3.7.0
PyOpenGL>=3.1.6
PyOpenGL-accelerate>=3.1.6

# Build tools
pyinstaller>=6.0.0

# FBX SDK
# Note: FBX Python SDK must be installed separately
# See INSTALL.md for installation instructions
# The fbx module is provided by Autodesk FBX Python SDK 2020.x
